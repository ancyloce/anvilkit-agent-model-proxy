apiVersion: v2
name: anvilkit-agent-model-proxy
description: The AnvilKit Agent Model Proxy (anvilkit-agent-model-proxy) - the service's own chart; environment values, route credentials and the pinned deployment combination belong to the deploying repository.
type: application
version: 0.1.0
appVersion: "0.1.0"
